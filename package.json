{
  "name": "webhook-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^4.11.7",
    "redis": "^5.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
